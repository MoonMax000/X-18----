# Руководство по перезапуску локального бэкенда

## ✅ Миграции применены успешно

Все миграции (024-028) были применены к локальной БД:
- ✅ Таблицы `subscriptions` и `post_purchases` созданы
- ✅ Поля `access_level` и `price_cents` добавлены в таблицу `posts`
- ✅ Поддержка значений 'public' и 'paid' для access_level

## 🔄 Как перезапустить бэкенд

### Шаг 1: Найти и остановить старый процесс

```bash
# Найти процесс на порту 8080
lsof -ti:8080

# Или найти все Go процессы
ps aux | grep "go run"

# Остановить процесс (замените PID на реальный ID)
kill <PID>

# Или принудительно остановить
kill -9 <PID>
```

**Альтернатива:** Нажмите Ctrl+C в терминале где запущен бэкенд.

### Шаг 2: Пересобрать бэкенд

```bash
cd custom-backend
go build -o server cmd/server/main.go
```

### Шаг 3: Запустить новый бэкенд

```bash
# Вариант 1: Запуск скомпилированного бинарника
./server

# Вариант 2: Запуск через go run
go run cmd/server/main.go
```

### Шаг 4: Проверить что бэкенд запустился

Вы должны увидеть:
```
✅ Database connected successfully
✅ Database migrations completed
✅ Redis connected successfully
✅ Stripe configured
🚀 Server running on http://0.0.0.0:8080
```

### Шаг 5: Перезагрузить фронтенд

В браузере нажмите `Ctrl+R` или `Cmd+R` для перезагрузки страницы.

## 🧪 Проверка работы

### Создайте платный пост:
1. Откройте композер поста
2. Выберите "Pay-per-post" (Users pay once to unlock this post)
3. Установите цену $5
4. Добавьте фото
5. Опубликуйте

### В консоли браузера вы должны увидеть:
```javascript
[FeedPost DEBUG] Post access control: {
  postId: "...",
  accessLevel: "pay-per-post",  // ✅ НЕ undefined
  priceCents: 500,               // ✅ НЕ undefined
  postPrice: 5.0,                // ✅ НЕ undefined
  isPurchased: false,
  isOwnPost: true,
  isLocked: false  // для автора
}
```

### Для другого пользователя:
- ✅ Пост должен отображаться как заблокированный
- ✅ Должна быть видна кнопка "Unlock for $5.00"
- ✅ Фото должно быть размытым

## 🔧 Если что-то не работает

### Проблема: Бэкенд не запускается
```bash
# Проверьте что порт освободился
lsof -ti:8080

# Проверьте что PostgreSQL запущен
psql -h localhost -U postgres -d x18_backend -c "SELECT 1"

# Проверьте логи
cd custom-backend
go run cmd/server/main.go
```

### Проблема: Поля все еще undefined
1. Убедитесь что бэкенд перезапущен с новым кодом
2. Очистите кэш браузера (Cmd+Shift+R)
3. Проверьте что в сети вкладке видны правильные JSON поля

### Проблема: Фото не отображается
1. Проверьте что медиа загружается успешно
2. Убедитесь что путь к изображению правильный
3. Проверьте консоль на ошибки загрузки

## 📊 Структура данных

После исправлений, POST должен иметь:
```json
{
  "id": "...",
  "content": "...",
  "accessLevel": "pay-per-post",  // camelCase
  "priceCents": 500,               // camelCase
  "postPrice": 5.0,                // вычисляется: priceCents / 100
  "isPurchased": false,            // проверяется в БД
  "isSubscriber": false,           // проверяется в БД
  "media": [...]
}
```

## ✅ Ожидаемый результат

После всех шагов:
- ✅ Бэкенд работает с новым кодом
- ✅ Миграции применены к БД
- ✅ Stripe настроен (тестовые ключи)
- ✅ Платные посты работают корректно
- ✅ Lock UI отображается правильно
- ✅ Фото видны в постах
