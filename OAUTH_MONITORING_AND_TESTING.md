# 🔍 Мониторинг и Тестирование OAuth

Полное руководство по мониторингу логов и тестированию OAuth авторизации через Google и Apple.

## 📊 Скрипты Мониторинга

### 1. Мониторинг Продакшен Логов

**Скрипт:** `./monitor-logs-production.sh`

#### Основные команды:

```bash
# Показать все логи (по умолчанию)
./monitor-logs-production.sh

# Показать только OAuth логи
./monitor-logs-production.sh --oauth

# Показать только ошибки
./monitor-logs-production.sh --errors

# Показать логи за последние 30 минут
MINUTES_AGO=30 ./monitor-logs-production.sh

# Непрерывный мониторинг (обновление каждые 10 секунд)
watch -n 10 ./monitor-logs-production.sh
```

#### Что отображается:

- ✅ **Успешные операции** (зеленый цвет)
- 🚨 **Ошибки** (красный цвет)
- ⚠️  **Предупреждения** (желтый цвет)
- 📱 **OAuth события** (голубой цвет)
- 📊 **Статистика** (количество запросов, ошибок, успешных операций)

#### Требования:

- AWS CLI установлен и настроен
- Права доступа к CloudWatch Logs (`logs:FilterLogEvents`)
- Доступ к ECS кластеру для проверки задач

---

### 2. Мониторинг Локальных Логов

**Скрипт:** `./monitor-logs-local.sh`

#### Основные команды:

```bash
# Показать последние 100 логов
./monitor-logs-local.sh

# Показать только OAuth логи (непрерывно)
./monitor-logs-local.sh --oauth

# Показать только ошибки (непрерывно)
./monitor-logs-local.sh --errors

# Непрерывный мониторинг всех логов
./monitor-logs-local.sh --follow

# Прямой доступ к логам Docker
docker logs -f <container_id>
```

#### Автоматическое определение источника:

Скрипт автоматически определяет:
1. ✅ Docker контейнер (если запущен)
2. ✅ Локальный Go процесс (если контейнер не найден)
3. ❌ Если ничего не найдено - выводит инструкции по запуску

---

## 🧪 Тестирование OAuth

### Продакшен (Production)

#### 🌐 URLs для тестирования:

| Сервис | URL |
|--------|-----|
| **Главная страница** | https://social.tyriantrade.com |
| **Google OAuth** | https://api.tyriantrade.com/api/auth/google |
| **Apple OAuth** | https://api.tyriantrade.com/api/auth/apple |
| **Health Check** | https://api.tyriantrade.com/health |

#### 🔑 OAuth Redirect URLs (настроены в консолях):

- **Google Console:** `https://api.tyriantrade.com/api/auth/google/callback`
- **Apple Console:** `https://api.tyriantrade.com/api/auth/apple/callback`

#### 📝 Процесс тестирования:

1. **Откройте главную страницу:**
   ```
   https://social.tyriantrade.com
   ```

2. **Нажмите на кнопку "Sign In with Google" или "Sign In with Apple"**

3. **Следите за логами в реальном времени:**
   ```bash
   # В отдельном терминале
   ./monitor-logs-production.sh --oauth
   ```

4. **После авторизации проверьте:**
   - ✅ Редирект обратно на фронтенд
   - ✅ Установка JWT токена в cookie
   - ✅ Отображение профиля пользователя

---

### Локальное окружение (Development)

#### 🌐 URLs для тестирования:

| Сервис | URL |
|--------|-----|
| **Frontend** | http://localhost:5173 |
| **Backend API** | http://localhost:8080 |
| **Health Check** | http://localhost:8080/health |
| **API Info** | http://localhost:8080/api |
| **Google OAuth** | http://localhost:8080/api/auth/google |
| **Apple OAuth** | http://localhost:8080/api/auth/apple |

#### 🔑 OAuth Redirect URLs (для локальной разработки):

- **Google Console:** `http://localhost:8080/api/auth/google/callback`
- **Apple Console:** `http://localhost:8080/api/auth/apple/callback`

> ⚠️ **Важно:** Убедитесь, что эти URL добавлены в настройки OAuth приложений в консолях Google и Apple!

#### 📝 Процесс тестирования:

1. **Запустите бэкенд:**
   ```bash
   ./START_CUSTOM_BACKEND_STACK.sh  # Docker
   # или
   cd custom-backend && go run cmd/server/main.go  # Напрямую
   ```

2. **Запустите фронтенд:**
   ```bash
   cd client && pnpm dev
   ```

3. **Откройте браузер:**
   ```
   http://localhost:5173
   ```

4. **Мониторинг логов:**
   ```bash
   # В отдельном терминале
   ./monitor-logs-local.sh --oauth
   ```

5. **Тестируйте авторизацию через Google/Apple**

---

## 🔍 Анализ Логов

### Что искать в логах при OAuth авторизации:

#### ✅ Успешная авторизация:

```
[HH:MM:SS] 🔐 Google OAuth: Initiating login...
[HH:MM:SS] ✅ Google OAuth callback received
[HH:MM:SS] ✅ User authenticated successfully
[HH:MM:SS] ✅ JWT token generated
[HH:MM:SS] ✅ Redirecting to frontend
```

#### ❌ Ошибки авторизации:

```
[HH:MM:SS] ❌ Failed to exchange code for token
[HH:MM:SS] ❌ Invalid OAuth state parameter
[HH:MM:SS] ❌ Failed to get user info from provider
[HH:MM:SS] ❌ Database error during user creation
```

### Типичные проблемы и решения:

| Проблема | Причина | Решение |
|----------|---------|---------|
| `invalid_client` | Неверный Client ID/Secret | Проверьте переменные окружения |
| `redirect_uri_mismatch` | URL не совпадает с настройками | Добавьте URL в консоль OAuth провайдера |
| `Invalid state` | Проблема с CSRF защитой | Проверьте настройки cookies и CORS |
| `email already exists` | Email уже зарегистрирован | Используйте линковку аккаунтов или другой метод входа |

---

## 🛠 Проверка Конфигурации

### Проверка переменных окружения (продакшен):

```bash
# Проверить наличие OAuth переменных в ECS задаче
aws ecs describe-task-definition \
  --task-definition tyriantrade-backend \
  --region us-east-1 \
  --query 'taskDefinition.containerDefinitions[0].environment' \
  | grep -E 'GOOGLE|APPLE|OAUTH'
```

### Проверка переменных окружения (локально):

```bash
# Проверить .env файл
cat custom-backend/.env | grep -E 'GOOGLE|APPLE|OAUTH'

# Или проверить в процессе
cd custom-backend && go run cmd/server/main.go
# При запуске должны появиться сообщения:
# ✅ Google OAuth configured (Client ID: ...)
# ✅ Apple OAuth configured (Client ID: ...)
```

---

## 📋 Чеклист для тестирования

### Перед началом тестирования:

- [ ] Backend запущен и доступен
- [ ] Frontend запущен и доступен
- [ ] Скрипты мониторинга имеют права на выполнение (`chmod +x`)
- [ ] AWS CLI настроен (для продакшена)
- [ ] OAuth приложения настроены в консолях Google и Apple
- [ ] Redirect URLs правильно указаны в консолях

### Тестирование Google OAuth:

- [ ] Кнопка "Sign In with Google" отображается
- [ ] Клик открывает страницу авторизации Google
- [ ] Выбор аккаунта работает корректно
- [ ] Разрешения запрашиваются корректно
- [ ] После авторизации происходит редирект обратно
- [ ] Пользователь залогинен и видит свой профиль
- [ ] JWT токен установлен в cookie
- [ ] В логах нет ошибок

### Тестирование Apple OAuth:

- [ ] Кнопка "Sign In with Apple" отображается
- [ ] Клик открывает страницу авторизации Apple
- [ ] Опция "Hide My Email" работает (если включена)
- [ ] После авторизации происходит редирект обратно
- [ ] Пользователь залогинен и видит свой профиль
- [ ] JWT токен установлен в cookie
- [ ] В логах нет ошибок

### После тестирования:

- [ ] Выход из системы работает корректно
- [ ] Повторный вход работает без проблем
- [ ] Сессии управляются правильно
- [ ] Refresh token работает (если реализован)

---

## 🚨 Troubleshooting

### Проблема: "No running tasks found" (продакшен)

**Решение:**
```bash
# Проверить статус сервиса
aws ecs describe-services \
  --cluster tyriantrade-cluster \
  --services tyriantrade-backend-service \
  --region us-east-1

# Если задач нет, проверить деплой
aws ecs list-tasks \
  --cluster tyriantrade-cluster \
  --region us-east-1
```

### Проблема: "Docker container not found" (локально)

**Решение:**
```bash
# Запустить контейнер
./START_CUSTOM_BACKEND_STACK.sh

# Проверить статус
docker ps | grep custom-backend

# Если контейнер не запускается, проверить логи
docker logs custom-backend
```

### Проблема: OAuth редирект не работает

**Решение:**
1. Проверьте URL в консолях OAuth провайдеров
2. Убедитесь, что CORS настроен правильно
3. Проверьте, что cookies включены в браузере
4. Проверьте логи на наличие ошибок

---

## 📚 Дополнительные ресурсы

- [Google OAuth Documentation](https://developers.google.com/identity/protocols/oauth2)
- [Apple Sign In Documentation](https://developer.apple.com/sign-in-with-apple/)
- [AWS CloudWatch Logs Documentation](https://docs.aws.amazon.com/cloudwatch/index.html)

---

## 🎯 Быстрый старт

### Тестирование в продакшене:

```bash
# 1. Откройте мониторинг логов
./monitor-logs-production.sh --oauth

# 2. В браузере откройте
open https://social.tyriantrade.com

# 3. Нажмите "Sign In with Google" или "Sign In with Apple"

# 4. Следите за логами в терминале
```

### Тестирование локально:

```bash
# 1. Запустите бэкенд
./START_CUSTOM_BACKEND_STACK.sh

# 2. В новом терминале запустите мониторинг
./monitor-logs-local.sh --oauth

# 3. В новом терминале запустите фронтенд
cd client && pnpm dev

# 4. Откройте браузер
open http://localhost:5173

# 5. Тестируйте OAuth авторизацию
```

---

**Дата создания:** 05.11.2025  
**Версия:** 1.0  
**Автор:** Development Team
