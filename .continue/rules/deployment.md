# Правила деплоя для X-18

## 🚫 ЗАПРЕЩЕНО

### НЕ используйте устаревшие платформы:
- ❌ Railway (railway.app)
- ❌ Netlify (netlify.com)
- ❌ Любые команды `railway ...`
- ❌ Любые команды `netlify ...`

### НЕ используйте неправильные команды билда:
- ❌ `pnpm run build` - билдит И клиент И сервер
- ❌ `npm run build` - то же самое
- ❌ Любые упоминания `dist/` без `dist/spa/`

## ✅ РАЗРЕШЕНО

### Используйте только AWS:
- ✅ AWS S3 + CloudFront для frontend
- ✅ AWS ECS + ECR для backend
- ✅ GitHub Actions для CI/CD

### Используйте правильные команды:

**Для frontend билда:**
```bash
pnpm run build:client  # Только клиент → dist/spa/
```

**Для деплоя:**
```bash
# Frontend
aws s3 sync dist/spa/ s3://tyriantrade-frontend/ --delete
aws cloudfront create-invalidation --distribution-id E2V60CFOUD2P7L --paths "/*"

# Backend
aws ecs update-service --cluster tyriantrade-cluster --service tyriantrade-backend-service --force-new-deployment
```

## 📄 Документация

### Главный документ:
- `DEPLOYMENT.md` - полная инструкция по деплою

### При вопросах о деплое:
1. Сначала прочитать `DEPLOYMENT.md`
2. Проверить `.github/workflows/deploy.yml`
3. НЕ использовать устаревшие MD файлы с упоминанием Railway/Netlify

## 🎯 Production URLs

- Frontend: https://social.tyriantrade.com
- API: https://api.tyriantrade.com

## 🔄 Git Workflow - ОБЯЗАТЕЛЬНЫЙ ПОРЯДОК ДЕЙСТВИЙ

### ⚠️ КРИТИЧЕСКИ ВАЖНО - Всегда начинать с синхронизации!

**ПЕРЕД началом любой работы:**
```bash
git pull origin main
```

**Это гарантирует:**
- У вас актуальная версия кода из production
- Нет конфликтов с изменениями других разработчиков
- Локальная и production версии синхронизированы

### Правильный порядок работы:

1. **Синхронизация (ОБЯЗАТЕЛЬНО ПЕРВЫМ ШАГОМ):**
   ```bash
   git pull origin main
   ```

2. **Делать изменения:**
   - Редактировать файлы локально
   - Тестировать локально (`pnpm run dev`)

3. **Проверить статус:**
   ```bash
   git status
   ```

4. **Добавить файлы:**
   ```bash
   git add <измененные-файлы>
   # или для всех файлов:
   git add .
   ```

5. **Закоммитить:**
   ```bash
   git commit -m "описание изменений"
   ```

6. **Запушить в production:**
   ```bash
   git push origin main
   ```

7. **Автоматический деплой:**
   - GitHub Actions автоматически запустится
   - Backend → AWS ECS
   - Frontend → AWS S3 + CloudFront
   - Время деплоя: ~5-10 минут

### ❌ Частые ошибки - НЕ ДЕЛАТЬ:

1. **НЕ пушить без git pull:**
   ```bash
   # ❌ НЕПРАВИЛЬНО:
   git add .
   git commit -m "..."
   git push origin main  # Без git pull!
   ```

2. **НЕ работать с устаревшей версией:**
   - Если не делали `git pull` сегодня
   - Если кто-то запушил изменения
   - Всегда сначала `git pull`!

3. **НЕ пушить временные файлы:**
   - `.pid` файлы
   - `.log` файлы
   - `.env.local`
   - `node_modules/` (уже в .gitignore)

### ✅ Правильная последовательность:

```bash
# 1. ВСЕГДА НАЧИНАТЬ С ЭТОГО:
git pull origin main

# 2. Делать изменения в файлах

# 3. Проверить что изменилось:
git status
git diff

# 4. Добавить только нужные файлы:
git add client/pages/SomePage.tsx
git add client/components/SomeComponent.tsx

# 5. Закоммитить с понятным сообщением:
git commit -m "feat: Add new feature to SomePage"

# 6. Запушить:
git push origin main

# 7. Дождаться деплоя (5-10 минут)
```

## ⚠️ Важно

При любых изменениях в процессе деплоя:
1. Обновить `DEPLOYMENT.md`
2. Обновить `.github/workflows/deploy.yml`
3. НЕ создавать новые Railway/Netlify конфигурации

### Проверка синхронизации:

Если сомневаетесь что у вас актуальная версия:
```bash
git fetch origin
git status  # покажет если ваша ветка отстает
git pull origin main  # синхронизировать
```
