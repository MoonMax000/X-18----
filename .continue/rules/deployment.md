# Правила деплоя и отчетности

## Почему первый деплой не применил изменения

### Проблема Docker кеша
GitHub Actions использует Docker layer caching для ускорения сборки. При первом деплое после изменений кода:
1. ✅ Код был правильно закоммичен (`c988479a`)
2. ✅ Docker образ собрался
3. ❌ **НО**: Docker использовал закешированный слой `RUN go build` из предыдущей сборки
4. ❌ Результат: новый код не попал в образ

### Решение
**Force rebuild** через empty commit:
```bash
git commit --allow-empty -m "Force rebuild: description"
git push origin main
```

Это заставляет Docker пересобрать ВСЕ слои заново, игнорируя кеш.

---

## Правила деплоя на production

### ✅ ПРАВИЛЬНЫЙ процесс:

1. **Закоммитить изменения**:
   ```bash
   git add <файлы>
   git commit -m "описание изменений"
   git push origin main
   ```

2. **ВАЖНО**: После критических изменений (библиотеки, зависимости, core логика):
   ```bash
   git commit --allow-empty -m "Force rebuild: <описание>"
   git push origin main
   ```

3. **Мониторинг деплоя**:
   ```bash
   gh run watch
   ```
   
   Или: https://github.com/MoonMax000/X-18----/actions

4. **Ждать завершения**: ECS deployment занимает 7-10 минут

5. **Подождать 1 минуту после ✅** перед тестированием

6. **Проверить логи**:
   ```bash
   ./check-apple-oauth-logs.sh
   ```

### ❌ ОШИБКИ которых избегать:

1. ❌ **НЕ тестировать** пока деплой не завершен
2. ❌ **НЕ предполагать** что деплой применил изменения - ВСЕГДА проверять логи
3. ❌ **НЕ создавать** множество промежуточных коммитов - лучше один с force rebuild

### 🔍 Проверка что деплой применился:

```bash
# Проверить последний коммит
git log --oneline -1

# Проверить что код в файле
cat <файл> | grep "<ключевая строка>"

# Проверить логи запуска на production
./check-apple-oauth-logs.sh
```

Ожидаемое: логи должны содержать признаки нового кода

---

## Структура отчетности (МАКСИМУМ 5 файлов)

### ИСПОЛЬЗУЕМ ТОЛЬКО эти 5 файлов для отчетов:

1. **`PROJECT_STATUS.md`** - Текущий статус проекта
   - Последние изменения
   - Текущие проблемы
   - Что работает / не работает
   - Следующие шаги

2. **`DEPLOYMENT_LOG.md`** - История деплоев
   - Дата + время
   - Что задеплоено
   - Результат (успех/ошибка)
   - Ссылки на GitHub Actions

3. **`TECHNICAL_DECISIONS.md`** - Технические решения
   - Почему выбрано решение X вместо Y
   - Архитектурные изменения
   - Важные имплементации

4. **`KNOWN_ISSUES.md`** - Известные проблемы
   - Активные баги
   - Workarounds
   - Планы по исправлению

5. **`QUICK_REFERENCE.md`** - Быстрая справка
   - Полезные команды
   - Скрипты
   - Endpoints
   - Credentials locations

### ✅ Правила работы с отчетами:

1. **ОБНОВЛЯТЬ**, а не создавать новые
2. **Добавлять** в начало файла (новое сверху)
3. **Удалять** устаревшую информацию
4. **Датировать** каждую запись: `## [2025-11-05 17:00]`
5. **Сохранять** только релевантную информацию

### ❌ НЕ создавать:
- `APPLE_OAUTH_DEBUG_REPORT.md`
- `APPLE_OAUTH_DIAGNOSIS_FINAL.md`
- `DEPLOYMENT_STATUS_2025_11_05.md`
- Другие временные отчеты

Вместо этого - ОБНОВЛЯТЬ существующие 5 файлов!

---

## Шаблон для PROJECT_STATUS.md

```markdown
# Project Status

## [YYYY-MM-DD HH:MM] Название изменения

### Статус
🔄 В процессе / ✅ Завершено / ❌ Проблема

### Что сделано
- Пункт 1
- Пункт 2

### Что работает
- Feature X
- Feature Y

### Известные проблемы
- Проблема Z (см. KNOWN_ISSUES.md)

### Следующие шаги
1. Шаг 1
2. Шаг 2

---
```

---

## Force Rebuild - когда использовать

### ✅ НУЖЕН Force Rebuild:
- Изменения в `go.mod` / `go.sum`
- Новые зависимости
- Изменения в core библиотеках
- Критические баг-фиксы
- Изменения в Dockerfile
- После неудачного деплоя

### ✴️ Опционально:
- Небольшие изменения UI
- Тексты / переводы
- CSS стили

### ❌ НЕ нужен:
- Изменения в README / документации
- Добавление комментариев
- Форматирование кода
