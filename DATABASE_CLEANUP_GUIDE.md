# 🗑️ Руководство по очистке базы данных

## Описание

Скрипт `cleanup-database.sh` позволяет безопасно удалить всех пользователей из базы данных, кроме админа. Скрипт удаляет:

- ✅ Всех пользователей (кроме admin)
- ✅ Все посты
- ✅ Все комментарии
- ✅ Все лайки
- ✅ Все подписки
- ✅ Все реферальные коды
- ✅ Все OAuth данные
- ✅ Все уведомления
- ✅ Все сессии

**⚠️ ВНИМАНИЕ**: Операция необратима! После выполнения восстановить данные будет невозможно.

## Требования

1. **PostgreSQL Client**: Убедитесь, что установлен `psql`:
   ```bash
   psql --version
   ```
   
   Если не установлен:
   ```bash
   # macOS
   brew install postgresql
   
   # Ubuntu/Debian
   sudo apt-get install postgresql-client
   ```

2. **Файлы конфигурации**: Должны существовать файлы с переменными окружения:
   - `.env.local` - для локальной базы данных
   - `.env.production` - для production базы данных

## Использование

### Для локальной базы данных

```bash
./cleanup-database.sh local
```

### Для production базы данных

```bash
./cleanup-database.sh production
```

## Процесс выполнения

1. **Скрипт покажет информацию о подключении:**
   ```
   🗑️  Скрипт очистки базы данных Tyrian Trade
   ============================================
   
   🚀 Окружение: PRODUCTION
   
   📡 Проверка подключения к базе данных...
      Host: your-rds-instance.amazonaws.com
      Port: 5432
      Database: tyriantrade_db
      User: admin
   ```

2. **Статистика:**
   ```
   📊 Статистика базы данных:
      Пользователей для удаления: 15
   ```

3. **Подтверждение:**
   ```
   ⚠️  Вы уверены, что хотите удалить 15 пользователей?
      Введите 'YES' для подтверждения:
   ```
   
   **ВАЖНО**: Введите именно `YES` (заглавными буквами), иначе операция будет отменена.

4. **Выполнение очистки:**
   ```
   🔄 Начинаю очистку базы данных...
   ```

5. **Результат:**
   ```
   ✅ База данных успешно очищена!
   
   📊 Оставшиеся пользователи:
    username | email              | role  | created_at
   ----------+--------------------+-------+-------------------------
    admin    | admin@example.com  | admin | 2024-01-01 00:00:00
   
   ✨ Готово!
   ```

## Отмена операции

Если вы передумали, просто введите любой текст кроме `YES` или нажмите `Ctrl+C`:

```
⚠️  Вы уверены, что хотите удалить 15 пользователей?
   Введите 'YES' для подтверждения: no
❌ Операция отменена
```

## Проверка результата

После выполнения скрипта, вы можете дополнительно проверить базу данных:

### Локально

```bash
psql -h localhost -U postgres -d x18_db -c "SELECT COUNT(*) FROM users;"
```

### Production (через AWS RDS)

```bash
psql -h your-rds-endpoint -U admin -d tyriantrade_db -c "SELECT COUNT(*) FROM users;"
```

Должен остаться только 1 пользователь (admin).

## Автоматизация очистки

### После каждого деплоя

Если вы хотите автоматически очищать базу после деплоя, добавьте в скрипт `deploy.sh`:

```bash
# В конце deploy.sh
echo "🗑️  Очистка тестовых пользователей..."
./cleanup-database.sh production
```

### Регулярная очистка (Cron)

Для регулярной очистки можно настроить cron job:

```bash
# Очистка каждую неделю в воскресенье в 3:00 AM
0 3 * * 0 cd /path/to/project && ./cleanup-database.sh production > /tmp/cleanup.log 2>&1
```

## Устранение проблем

### Ошибка: "psql: command not found"

**Решение**: Установите PostgreSQL client:
```bash
brew install postgresql  # macOS
```

### Ошибка: "Не удалось подключиться к базе данных"

**Причины:**
1. Неправильные данные в `.env.production`
2. База данных недоступна
3. Неправильный пароль

**Решение**: Проверьте переменные окружения:
```bash
cat .env.production | grep AWS_RDS
```

### Ошибка: "Permission denied"

**Решение**: Сделайте скрипт исполняемым:
```bash
chmod +x cleanup-database.sh
```

### Ошибка: "Admin user not found"

Если админ не существует, создайте его:

```bash
# Локально
psql -h localhost -U postgres -d x18_db < custom-backend/internal/database/migrations/021_create_permanent_admin.sql

# Production
./create-admin-production.sh
```

## Безопасность

### Резервное копирование

**Перед очисткой production всегда делайте бэкап:**

```bash
# Создание бэкапа
pg_dump -h your-rds-endpoint -U admin -d tyriantrade_db > backup_$(date +%Y%m%d_%H%M%S).sql

# Восстановление из бэкапа (если нужно)
psql -h your-rds-endpoint -U admin -d tyriantrade_db < backup_20240101_120000.sql
```

### Двойное подтверждение

Скрипт требует явного ввода `YES` для предотвращения случайного запуска.

### Транзакционность

Все операции выполняются в транзакции `BEGIN ... COMMIT`, поэтому в случае ошибки изменения автоматически откатятся.

## Альтернативные методы

### Через TablePlus (GUI)

1. Подключитесь к базе данных
2. Откройте файл `cleanup-all-except-admin.sql`
3. Выполните запросы вручную

### Через AWS ECS Exec

```bash
./cleanup-production-db-via-ecs.sh
```

### Через AWS RDS Console (Query Editor)

1. Откройте AWS RDS Console
2. Выберите Query Editor
3. Подключитесь к базе данных
4. Вставьте содержимое `cleanup-all-except-admin.sql`
5. Выполните запросы

## Часто задаваемые вопросы

### Q: Можно ли отменить удаление?
**A**: Нет, операция необратима. Сделайте бэкап перед выполнением.

### Q: Сколько времени занимает очистка?
**A**: Обычно 5-30 секунд в зависимости от количества данных.

### Q: Удаляются ли медиа-файлы (изображения)?
**A**: Нет, скрипт удаляет только записи в базе данных. Медиа-файлы в S3 остаются.

### Q: Можно ли сохранить определенных пользователей?
**A**: Да, отредактируйте условие в SQL: `WHERE username NOT IN ('admin', 'testuser')`

### Q: Что делать если скрипт завис?
**A**: Нажмите `Ctrl+C` для прерывания. Транзакция автоматически откатится.

## Поддержка

При возникновении проблем:

1. Проверьте логи: `cat /tmp/cleanup.log`
2. Проверьте подключение к БД
3. Убедитесь что админ существует
4. Создайте issue в GitHub

## История изменений

- **v1.0.0** (08.11.2025): Первая версия скрипта
  - Поддержка local и production окружений
  - Безопасное удаление с подтверждением
  - Транзакционность
  - Детальная статистика
