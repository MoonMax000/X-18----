# Инструкции по тестированию OAuth Fix

## Что было исправлено

Исправлена критическая проблема: пользователь НЕ оставался залогиненным после OAuth авторизации (Google/Apple).

### Изменённые файлы
- ✅ `client/contexts/AuthContext.tsx` - исправлена логика инициализации для работы с httpOnly cookies

## Локальное тестирование

### Текущее состояние
- ✅ Backend запущен (port 8080)
- ✅ Frontend запущен (port 5173)

### Шаги тестирования

1. **Откройте браузер**
   ```
   http://localhost:5173
   ```

2. **Откройте DevTools**
   - Нажмите F12 или Cmd+Option+I (Mac)
   - Перейдите во вкладку "Console"
   - ✅ ВАЖНО: Включите "Preserve log" чтобы логи не исчезали при редиректах

3. **Авторизуйтесь через OAuth**
   - Нажмите "Sign In" в правом верхнем углу
   - Выберите "Continue with Google" или "Continue with Apple"
   - Войдите в свой аккаунт Google/Apple
   - Дайте разрешения приложению

4. **Проверьте логи в Console**
   
   Вы должны увидеть последовательность:
   ```
   === OAuth Callback Handler ===
   Success: true
   ✅ OAuth successful! Tokens are in httpOnly cookies
   ✅ User data fetched: [ваш username]
   🚀 Redirecting to home...
   [После редиректа]
   🔍 Checking authentication with cookies...
   ✅ Auth initialized successfully (via cookies)
   ```

5. **Проверьте что вы авторизованы**
   - ✅ В правом верхнем углу отображается ваше имя/аватар вместо "Sign In"
   - ✅ Можете создавать посты
   - ✅ Обновление страницы (F5) не разлогинивает вас
   - ✅ Можете открыть свой профиль

6. **Проверьте cookies в DevTools**
   - Перейдите в Application → Cookies → http://localhost:5173
   - Должны быть cookies:
     - `access_token` (HttpOnly ✅)
     - `refresh_token` (HttpOnly ✅)

### Если что-то пошло не так

#### Проблема: "Failed to fetch user data"
```bash
# Проверьте что backend запущен
ps aux | grep "go run"

# Если не запущен, запустите:
cd custom-backend
go run cmd/server/main.go
```

#### Проблема: Vite ошибки
```bash
# Перезапустите frontend
pnpm run dev
```

#### Проблема: OAuth редирект не работает
Проверьте что в `custom-backend/.env`:
```env
FRONTEND_URL=http://localhost:5173
GOOGLE_REDIRECT_URI=http://localhost:8080/api/auth/google/callback
APPLE_REDIRECT_URI=http://localhost:8080/api/auth/apple/callback
```

## Production Deployment

### Предварительные требования
- ✅ Все изменения закоммичены в git
- ✅ Локально всё протестировано и работает

### Деплой

1. **Коммит изменений**
   ```bash
   git add client/contexts/AuthContext.tsx
   git add OAUTH_AUTH_PERSISTENCE_FIX.md
   git add OAUTH_FIX_TEST_INSTRUCTIONS.md
   git commit -m "fix: OAuth authentication persistence after redirect"
   git push origin main
   ```

2. **Задеплойте в production**
   ```bash
   ./deploy.sh
   ```

3. **Мониторинг деплоя**
   ```bash
   # Следите за статусом в AWS Console или:
   aws ecs describe-services \
     --cluster tyriantrade-cluster \
     --services tyriantrade-backend-service \
     --region eu-central-1
   ```

### Production тестирование

1. **Откройте сайт**
   ```
   https://tyriantrade.com
   ```

2. **Откройте DevTools → Console**
   - ✅ Включите "Preserve log"

3. **Авторизуйтесь через Google или Apple**

4. **Проверьте что остались залогиненными**
   - Имя отображается в header
   - F5 не разлогинивает
   - Можете создавать посты

5. **Мониторинг логов**
   ```bash
   # В другом терминале запустите:
   ./monitor-logs-production.sh
   ```

### Ожидаемое поведение в Production

При успешной авторизации в логах CloudWatch должно быть:
```
OAuth callback successful for user: [username]
User [username] logged in successfully via oauth
```

В браузере Console:
```
🔍 Checking authentication with cookies...
✅ Auth initialized successfully (via cookies)
```

## Откат (если что-то пошло не так)

Если в production возникли проблемы:

1. **Откатите деплой**
   ```bash
   git revert HEAD
   git push origin main
   ./deploy.sh
   ```

2. **Или задеплойте предыдущий коммит**
   ```bash
   git checkout <предыдущий-коммит-hash>
   ./deploy.sh
   git checkout main
   ```

## Проверка регрессий

Убедитесь что другие методы авторизации продолжают работать:

### Email/Password авторизация
1. Создайте новый аккаунт через email
2. Подтвердите email код
3. Залогиньтесь
4. ✅ Должны остаться залогиненными

### Logout
1. Нажмите на аватар → Logout
2. ✅ Должны разлогиниться
3. ✅ Cookies должны удалиться

### Refresh Token
1. Залогиньтесь
2. Подождите 15 минут (истечение access token)
3. Попробуйте выполнить действие (создать пост)
4. ✅ Токен должен автоматически обновиться
5. ✅ Действие должно выполниться успешно

## Контакты для поддержки

Если возникли проблемы:
1. Проверьте логи: `./monitor-logs-production.sh`
2. Проверьте browser console (с "Preserve log")
3. Предоставьте скриншоты ошибок

## Статус
🚀 ГОТОВО К ТЕСТИРОВАНИЮ

Дата: 5 ноября 2025, 14:11 UTC+7
