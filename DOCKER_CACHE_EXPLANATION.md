# Docker Cache - Как это работает

## ⚠️ Важно понять

Docker cache с BuildKit работает **начиная со ВТОРОГО деплоя**, а не с первого!

### Первый деплой (текущий)
```
Коммит c2c90261
    ↓
GitHub Actions запускается
    ↓
Docker Buildx сборка БЕЗ кэша
    ↓
Создается кэш слоев (cache layers)
    ↓
Образ пушится в ECR
    ↓
Время: ~8-10 минут (как обычно)
```

### Второй и последующие деплои
```
Следующий коммит
    ↓
GitHub Actions запускается
    ↓
Docker Buildx находит кэш в ECR
    ↓
Использует кэшированные слои
    ↓
Пересобирает только измененные слои
    ↓
Время: ~2-3 минуты (ускорение!)
```

## 📊 Как это работает

### Что кэшируется:
1. ✅ `go mod download` - зависимости Go
2. ✅ Компиляция Go бинарника
3. ✅ Alpine base image
4. ✅ Установка ca-certificates

### Что НЕ кэшируется:
- ❌ Изменения в исходном коде
- ❌ Новые зависимости
- ❌ Изменения в Dockerfile

## 🔍 Проверка эффективности

После следующего деплоя проверьте:

```bash
# Посмотреть время последних деплоев
gh run list --limit 5

# Сравнить время:
# - Первый деплой (c2c90261): ~8-10 минут
# - Второй деплой: должно быть ~2-3 минуты
```

## 💡 Почему именно так?

**Технически:**
```yaml
docker buildx build \
  --cache-from=$ECR_REGISTRY/$ECR_REPOSITORY:latest \  # ← Ищет кэш здесь
  --build-arg BUILDKIT_INLINE_CACHE=1 \               # ← Создает кэш
  --push
```

1. `--cache-from` смотрит в ECR на образ `latest`
2. Если там есть слои с нашими командами → использует их
3. `BUILDKIT_INLINE_CACHE=1` сохраняет метаданные кэша в образ
4. При следующей сборке эти слои уже будут доступны

## 🎯 Ожидаемые результаты

### Сценарий 1: Изменения только в коде
```
Время сборки: ~2-3 минуты
Что пересобирается:
- ✅ Только компиляция измененных файлов
- ❌ НЕ качаются зависимости (кэш)
- ❌ НЕ устанавливаются пакеты (кэш)
```

### Сценарий 2: Добавлены новые зависимости
```
Время сборки: ~5-6 минут
Что пересобирается:
- ✅ go mod download
- ✅ Компиляция с новыми зависимостями
- ❌ НЕ устанавливаются базовые пакеты (кэш)
```

### Сценарий 3: Изменен Dockerfile
```
Время сборки: ~8-10 минут
Что пересобирается:
- ✅ Все с нуля
- ❌ Кэш недействителен
```

## 📈 Отслеживание эффективности

Создайте файл для отслеживания времени деплоев:

```bash
# После каждого деплоя запускайте:
echo "$(date '+%Y-%m-%d %H:%M') - $(gh run list --limit 1 --json conclusion,durationMs --jq '.[0].durationMs/60000') min" >> deploy-times.log
```

Через 5-10 деплоев вы увидите статистику:
```
2025-11-08 00:34 - 9.2 min  ← Первый (создание кэша)
2025-11-08 10:15 - 2.8 min  ← Второй (использование кэша) ✅
2025-11-08 14:30 - 2.5 min  ← Третий (использование кэша) ✅
2025-11-08 18:45 - 8.9 min  ← Изменен Dockerfile
2025-11-09 09:00 - 2.7 min  ← Снова быстро ✅
```

## 🔄 Нужно ли чистить кэш вручную?

### ❌ НЕТ! Docker BuildKit делает это автоматически

**Как работает умная инвалидация кэша:**

### При изменении бэкенда (Go код):
```
Изменили: custom-backend/internal/api/auth.go
    ↓
Docker видит изменение ПОСЛЕ команды COPY
    ↓
Кэш до COPY - используется ✅
   - go mod download (кэшировано)
   - Alpine image (кэшировано)
    ↓
Кэш после COPY - инвалидирован ❌
   - Пересобирается только компиляция
    ↓
Время: ~2-3 минуты (вместо 10)
```

### При изменении зависимостей:
```
Изменили: go.mod или go.sum
    ↓
Docker видит изменение В команде COPY go.mod
    ↓
Кэш go mod download - инвалидирован ❌
    ↓
Качает новые зависимости
Компилирует заново
    ↓
Время: ~5-6 минут
```

### При изменении фронтенда:
```
Фронтенд деплоится в S3 + CloudFront
    ↓
НЕ использует Docker кэш
    ↓
Но нужна инвалидация CloudFront CDN
    ↓
Уже настроено в .github/workflows/deploy.yml:
```

```yaml
- name: Invalidate CloudFront
  run: |
    aws cloudfront create-invalidation \
      --distribution-id $CLOUDFRONT_DISTRIBUTION_ID \
      --paths "/*"
```

## 🎯 Когда НУЖНА ручная очистка

### 1. CloudFront CDN (фронтенд) - УЖЕ АВТОМАТИЧЕСКИ
```bash
# Уже в deploy.yml, но если нужно вручную:
aws cloudfront create-invalidation \
  --distribution-id E2V60CFOUD2P7L \
  --paths "/*"
```

### 2. Browser cache (у пользователей)
Настроено в deploy.yml:
```yaml
# index.html - всегда свежий
--cache-control "public, max-age=0, must-revalidate"

# JS/CSS файлы - кэшируются с hash в имени
--cache-control "public, max-age=31536000, immutable"
```

### 3. Docker кэш (если что-то пошло не так)
```bash
# Только если есть проблемы, используйте:
docker buildx build --no-cache ...

# Или в .github/workflows/deploy.yml временно уберите:
# --cache-from=$ECR_REGISTRY/$ECR_REPOSITORY:latest
```

## 📋 Примеры реальных сценариев

### Сценарий 1: Изменили один файл бэкенда
```
Файл: custom-backend/internal/api/users.go
Изменение: добавили новый endpoint

Docker кэш:
✅ go mod download (используется)
✅ Alpine base (используется)
❌ Компиляция (пересобирается)

Результат: ~2-3 минуты
Действия: НИЧЕГО, все автоматически
```

### Сценарий 2: Обновили зависимость бэкенда
```
Файл: go.mod
Изменение: добавили новую библиотеку

Docker кэш:
✅ Alpine base (используется)
❌ go mod download (инвалидирован)
❌ Компиляция (инвалидирована)

Результат: ~5-6 минут
Действия: НИЧЕГО, все автоматически
```

### Сценарий 3: Изменили фронтенд
```
Файл: client/components/auth/LoginModal.tsx
Изменение: изменили UI

Docker кэш: НЕ ИСПОЛЬЗУЕТСЯ (фронтенд в S3)
CloudFront: автоматическая инвалидация в deploy.yml

Результат: ~3-4 минуты (pnpm build)
Действия: НИЧЕГО, CloudFront инвалидируется автоматически
```

### Сценарий 4: Изменили и бэк и фронт
```
Изменения:
- custom-backend/internal/api/auth.go
- client/pages/LoginPage.tsx

Deploy jobs запускаются параллельно:
- deploy-backend: использует Docker кэш
- deploy-frontend: инвалидирует CloudFront

Результат: ~3-4 минуты (параллельно)
Действия: НИЧЕГО, все автоматически
```

## ⚡ Когда кэш НЕ помогает

1. **Изменен Dockerfile** - полная пересборка (~10 минут)
2. **Изменен go.mod/go.sum** - частичная пересборка (~5-6 минут)
3. **Первый деплой** - создание кэша (~10 минут)

## ✅ Вывод

**Текущий деплой (первый):** ~8-10 минут - это нормально!
**Следующие деплои:** будут ~2-3 минуты

**Ручная очистка кэша НЕ нужна:**
- ✅ Docker кэш - автоматически инвалидируется при изменениях
- ✅ CloudFront CDN - автоматически инвалидируется в pipeline
- ✅ Browser cache - правильно настроен (max-age=0 для index.html)

Терпение! Кэш создается прямо сейчас. 🚀
