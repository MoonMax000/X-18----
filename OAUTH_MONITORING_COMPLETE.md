# 🔍 OAuth Monitoring & Testing - Полное Руководство

## 📋 Обзор

Этот документ содержит полную инструкцию по мониторингу OAuth авторизации через Google и Apple в продакшене и локальной разработке.

---

## 🚀 Быстрый Старт

### Продакшен (Production)

```bash
# Просмотр последних 15 минут логов
./monitor-oauth-production-enhanced.sh

# Непрерывный мониторинг (real-time)
./monitor-oauth-production-enhanced.sh --follow
```

### Локальная Разработка (Local Development)

```bash
# Проверка статуса OAuth
./monitor-oauth-local-enhanced.sh

# Непрерывный мониторинг
./monitor-oauth-local-enhanced.sh --follow
```

---

## 🔗 URLs для Тестирования

### 🌐 Продакшен

| Провайдер | URL | Действие |
|-----------|-----|----------|
| **Google OAuth** | https://social.tyriantrade.com/ | Нажмите "Sign in with Google" |
| **Apple OAuth** | https://social.tyriantrade.com/ | Нажмите "Sign in with Apple" |
| **API Backend** | https://api.tyriantrade.com/ | API endpoint |

### 💻 Локальная Разработка

| Провайдер | URL | Действие |
|-----------|-----|----------|
| **Google OAuth** | http://localhost:5173/ | Нажмите "Sign in with Google" |
| **Apple OAuth** | http://localhost:5173/ | Нажмите "Sign in with Apple" |
| **API Backend** | http://localhost:8080/ | API endpoint |

---

## 📊 Скрипты Мониторинга

### 1. monitor-oauth-production-enhanced.sh

**Назначение:** Мониторинг OAuth в продакшене через AWS CloudWatch Logs

**Возможности:**
- ✅ Проверка статуса ECS сервиса
- ✅ Отображение OAuth конфигурации
- ✅ Логи OAuth запросов (Google, Apple)
- ✅ Логи ошибок
- ✅ Логи успешных авторизаций
- ✅ Цветная подсветка ключевых слов
- ✅ Режим непрерывного мониторинга

**Использование:**
```bash
# Статичный режим - показывает последние 15 минут
./monitor-oauth-production-enhanced.sh

# Режим реального времени (с автообновлением каждые 2 сек)
./monitor-oauth-production-enhanced.sh --follow
```

**Требования:**
- AWS CLI установлен и настроен
- Доступ к кластеру `tyriantrade-cluster`
- Права на чтение CloudWatch Logs (`/ecs/tyriantrade-backend`)

---

### 2. monitor-oauth-local-enhanced.sh

**Назначение:** Мониторинг OAuth в локальной разработке

**Возможности:**
- ✅ Проверка, запущен ли бэкенд (порт 8080)
- ✅ Проверка конфигурации OAuth из .env файла
- ✅ Проверка наличия Apple Private Key
- ✅ Тестирование OAuth endpoints
- ✅ Чтение логов из файла (если настроено)
- ✅ Режим непрерывного мониторинга

**Использование:**
```bash
# Проверка статуса и последних логов
./monitor-oauth-local-enhanced.sh

# Непрерывный мониторинг (требуется server.log файл)
./monitor-oauth-local-enhanced.sh --follow
```

**Требования:**
- Бэкенд запущен: `cd custom-backend && ./START_CUSTOM_BACKEND_STACK.sh`
- Файл .env настроен в `custom-backend/.env`
- (Опционально) Логи в файл: `go run cmd/server/main.go 2>&1 | tee server.log`

---

## 🛠 Конфигурация OAuth

### Google OAuth

**Продакшен:**
```env
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URL=https://api.tyriantrade.com/api/auth/google/callback
```

**Локальная разработка:**
```env
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URL=http://localhost:8080/api/auth/google/callback
```

### Apple OAuth

**Продакшен:**
```env
APPLE_CLIENT_ID=com.tyriantrade.app
APPLE_TEAM_ID=CYAMCL7B32
APPLE_KEY_ID=38Q92R9767
APPLE_PRIVATE_KEY_PATH=/app/apple-keys/AuthKey_38Q92R9767.p8
APPLE_REDIRECT_URL=https://api.tyriantrade.com/api/auth/apple/callback
```

**Локальная разработка:**
```env
APPLE_CLIENT_ID=com.tyriantrade.app
APPLE_TEAM_ID=CYAMCL7B32
APPLE_KEY_ID=38Q92R9767
APPLE_PRIVATE_KEY_PATH=/app/apple-keys/AuthKey_38Q92R9767.p8
APPLE_REDIRECT_URL=http://localhost:8080/api/auth/apple/callback
```

---

## 🔍 Типичные Проблемы и Решения

### 1. "Invalid state parameter"

**Причина:** State токен истёк (10 минут) или несколько вкладок используют один state

**Решение:**
- Закройте все вкладки с OAuth
- Очистите кеш браузера
- Попробуйте авторизоваться заново

### 2. Apple "Регистрация не выполнена"

**Возможные причины:**
- Service ID не настроен в Apple Developer Console
- Domain не добавлен в Domains and Subdomains
- Private Key не найден или неправильный путь
- Client ID не совпадает

**Проверка:**
```bash
# Продакшен
./check-production-oauth.sh

# Локально
./monitor-oauth-local-enhanced.sh
```

### 3. Google OAuth возвращает 409 Conflict

**Причина:** Email уже зарегистрирован, нужно связать аккаунт

**Решение:** Фронтенд должен обработать 409 статус и показать AccountLinkingModal
_(требуется доработка фронтенда)_

### 4. "No email provided by OAuth provider"

**Причина:** Пользователь не дал разрешение на доступ к email

**Решение:** Попросите пользователя предоставить доступ к email при авторизации

---

## 📈 Мониторинг в Реальном Времени

### Продакшен - AWS CloudWatch

```bash
# Все логи в реальном времени
aws logs tail /ecs/tyriantrade-backend --follow

# Только OAuth логи
./monitor-oauth-production-enhanced.sh --follow

# Только ошибки
aws logs tail /ecs/tyriantrade-backend --follow --filter-pattern "ERROR"
```

### Локально - Terminal Logs

```bash
# Запустите бэкенд с выводом в файл
cd custom-backend
go run cmd/server/main.go 2>&1 | tee server.log

# В другом терминале запустите мониторинг
./monitor-oauth-local-enhanced.sh --follow
```

---

## 🧪 Тестирование OAuth Flow

### Шаг 1: Запуск мониторинга

```bash
# Продакшен
./monitor-oauth-production-enhanced.sh --follow

# Локально
./monitor-oauth-local-enhanced.sh --follow
```

### Шаг 2: Откройте браузер

**Продакшен:** https://social.tyriantrade.com/  
**Локально:** http://localhost:5173/

### Шаг 3: Нажмите кнопку OAuth

Выберите "Sign in with Google" или "Sign in with Apple"

### Шаг 4: Наблюдайте за логами

В терминале с мониторингом вы увидите:
- 🔵 OAuth Login запрос
- 🟢 Успешная авторизация
- 🔴 Ошибки (если есть)

### Шаг 5: Проверьте результат

После успешной авторизации вы должны:
- Быть перенаправлены на главную страницу
- Видеть свой профиль в правом верхнем углу
- Иметь доступ к защищенным функциям

---

## 📝 Логи: Что Искать

### ✅ Успешная Конфигурация

```
✅ Apple OAuth configured: ClientID=com.tyriantrade.app, TeamID=CYAMCL7B32
✅ Google OAuth configured (OIDC): ClientID=659860...
```

### ✅ Успешный OAuth Request

```
=== Apple OAuth Callback Started ===
Request Method: POST
Code present: true, State present: true
State verification - found: true, provider: apple
Token exchange successful
Parsed Apple user: Sub=..., Email=..., EmailVerified=true
OAuth login successful for user: ...
```

### ❌ Ошибки

```
ERROR: Invalid state parameter
ERROR: Failed to exchange token
ERROR: Email not provided by OAuth provider
ERROR: Failed to parse Apple ID token
```

---

## 🔐 Безопасность

### State Token
- Генерируется случайным образом (32 байта)
- Хранится в Redis с TTL 10 минут
- Проверяется на стороне сервера
- Удаляется после использования

### Private Keys
- Apple Private Key хранится в `/app/apple-keys/` в контейнере
- Не коммитится в Git (проверьте .gitignore)
- Имеет права доступа 600

### Sessions
- Refresh token хешируется с SHA-256 + bcrypt
- Хранится в БД с JTI для ротации
- HttpOnly cookie для Refresh Token
- Access Token в localStorage (короткий TTL)

---

## 📞 Поддержка

### Проблемы с Apple OAuth?

1. Проверьте конфигурацию: `./check-production-oauth.sh`
2. Проверьте логи: `./monitor-oauth-production-enhanced.sh`
3. Проверьте Apple Developer Console:
   - Service IDs
   - Domains and Subdomains
   - Keys

### Проблемы с Google OAuth?

1. Проверьте Google Cloud Console
2. Проверьте Authorized redirect URIs
3. Проверьте логи мониторинга

### Общие Проблемы

- Очистите кеш браузера
- Проверьте переменные окружения
- Проверьте логи AWS/локальные
- Удалите тестовых пользователей и попробуйте заново

---

## 🚀 Следующие Шаги

### Текущие Известные Проблемы

1. **Apple OAuth**: Показывает "Регистрация не выполнена"
   - Требуется проверка логов для точной диагностики
   - Возможно связано с Service ID конфигурацией

2. **Google OAuth**: Фронтенд не обрабатывает 409 Conflict
   - Требуется создание `AccountLinkingModal` компонента
   - Требуется обновление `OAuthCallback.tsx`

### План Доработки

- [ ] Исправить Apple OAuth на продакшене
- [ ] Реализовать AccountLinkingModal для Google OAuth
- [ ] Добавить обработку 409 статуса на фронтенде
- [ ] Добавить тесты для OAuth flow
- [ ] Документировать процесс добавления новых OAuth провайдеров

---

## 📚 Дополнительные Ресурсы

- [OAUTH_DEPLOYMENT_GUIDE.md](./OAUTH_DEPLOYMENT_GUIDE.md) - Деплой OAuth конфигурации
- [APPLE_OAUTH_PRODUCTION_SETUP.md](./APPLE_OAUTH_PRODUCTION_SETUP.md) - Настройка Apple OAuth
- [OAUTH_TESTING_GUIDE.md](./OAUTH_TESTING_GUIDE.md) - Руководство по тестированию

---

## ✅ Checklist

### Продакшен
- [x] AWS CLI настроен
- [x] Google OAuth настроен
- [x] Apple OAuth настроен (с ошибкой)
- [x] CloudWatch Logs доступны
- [x] Мониторинг скрипты созданы
- [ ] Apple OAuth работает без ошибок
- [ ] Фронтенд обрабатывает 409 статус

### Локальная Разработка
- [x] Бэкенд запускается
- [x] .env файл настроен
- [x] Google OAuth тестируется
- [x] Apple OAuth тестируется
- [x] Мониторинг скрипт создан

---

**Последнее обновление:** 04.11.2025  
**Версия:** 1.0
